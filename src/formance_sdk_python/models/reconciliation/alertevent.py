"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from datetime import datetime
from enum import Enum
from formance_sdk_python.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
import pydantic
from pydantic import model_serializer
from typing import Any, Dict, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class NewStatus(str, Enum):
    OPEN = "OPEN"
    ACKNOWLEDGED = "ACKNOWLEDGED"
    RESOLVED = "RESOLVED"


class PrevStatus(str, Enum):
    OPEN = "OPEN"
    ACKNOWLEDGED = "ACKNOWLEDGED"
    RESOLVED = "RESOLVED"


class Type(str, Enum):
    FAIL = "fail"
    PASS = "pass"
    ACK = "ack"
    RESOLVE = "resolve"
    ACCEPT = "accept"
    SNOOZE = "snooze"
    UNSNOOZE = "unsnooze"


class AlertEventTypedDict(TypedDict):
    r"""One row of the alert's append-only event log. Captures every
    evaluation that touched the alert plus every manual transition
    (ack / resolve / accept / snooze / unsnooze). `prevStatus` is null
    only for the inaugural event of an alert. `isReopen` is a derived
    predicate — true when a FAIL event lands on a previously-RESOLVED alert.
    `notify` is the notification decision recorded for the row: false marks
    a transition kept for audit but deliberately not published (a repeated
    identical fail, or a fail muted by an active snooze).

    """

    alert_id: str
    at: datetime
    id: str
    is_reopen: bool
    new_status: NewStatus
    notify: bool
    r"""Whether this transition was published to the message bus. false = recorded for audit but suppressed."""
    type: Type
    evaluation_id: NotRequired[Nullable[str]]
    payload: NotRequired[Dict[str, Any]]
    r"""Event-type-specific payload (evidence for fail, resolution for resolve/accept, ack object for ack, snooze object for snooze)."""
    prev_status: NotRequired[Nullable[PrevStatus]]


class AlertEvent(BaseModel):
    r"""One row of the alert's append-only event log. Captures every
    evaluation that touched the alert plus every manual transition
    (ack / resolve / accept / snooze / unsnooze). `prevStatus` is null
    only for the inaugural event of an alert. `isReopen` is a derived
    predicate — true when a FAIL event lands on a previously-RESOLVED alert.
    `notify` is the notification decision recorded for the row: false marks
    a transition kept for audit but deliberately not published (a repeated
    identical fail, or a fail muted by an active snooze).

    """

    alert_id: Annotated[str, pydantic.Field(alias="alertID")]

    at: datetime

    id: str

    is_reopen: Annotated[bool, pydantic.Field(alias="isReopen")]

    new_status: Annotated[NewStatus, pydantic.Field(alias="newStatus")]

    notify: bool
    r"""Whether this transition was published to the message bus. false = recorded for audit but suppressed."""

    type: Type

    evaluation_id: Annotated[
        OptionalNullable[str], pydantic.Field(alias="evaluationID")
    ] = UNSET

    payload: Optional[Dict[str, Any]] = None
    r"""Event-type-specific payload (evidence for fail, resolution for resolve/accept, ack object for ack, snooze object for snooze)."""

    prev_status: Annotated[
        OptionalNullable[PrevStatus], pydantic.Field(alias="prevStatus")
    ] = UNSET

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["evaluationID", "payload", "prevStatus"])
        nullable_fields = set(["evaluationID", "prevStatus"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


try:
    AlertEvent.model_rebuild()
except NameError:
    pass
