"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from datetime import datetime
from enum import Enum
from formance_sdk_python.types import BaseModel, UNSET_SENTINEL
import pydantic
from pydantic import ConfigDict, model_serializer
from typing import Any, Dict, List, Optional, Union
from typing_extensions import Annotated, NotRequired, TypeAliasType, TypedDict


class EvidenceTypedDict(TypedDict):
    evidence: NotRequired[Dict[str, Any]]
    r"""Full breakdown. Present on FAIL entries."""
    fingerprint: NotRequired[str]
    passed: NotRequired[bool]
    proof: NotRequired[Dict[str, str]]
    r"""Compact green-proof (observed balance integers as strings). Present on PASS entries."""


class Evidence(BaseModel):
    model_config = ConfigDict(
        populate_by_name=True, arbitrary_types_allowed=True, extra="allow"
    )
    __pydantic_extra__: Dict[str, Any] = pydantic.Field(init=False)

    evidence: Optional[Dict[str, Any]] = None
    r"""Full breakdown. Present on FAIL entries."""

    fingerprint: Optional[str] = None

    passed: Optional[bool] = None

    proof: Optional[Dict[str, str]] = None
    r"""Compact green-proof (observed balance integers as strings). Present on PASS entries."""

    @property
    def additional_properties(self):
        return self.__pydantic_extra__

    @additional_properties.setter
    def additional_properties(self, value):
        self.__pydantic_extra__ = value  # pyright: ignore[reportIncompatibleVariableOverride]

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["evidence", "fingerprint", "passed", "proof"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            serialized.pop(k, serialized.pop(n, None))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val
        for k, v in serialized.items():
            m[k] = v

        return m


EvidenceUnionTypedDict = TypeAliasType(
    "EvidenceUnionTypedDict", Union[List[EvidenceTypedDict], Dict[str, Any]]
)
r"""For PASS/FAIL: the per-fingerprint roster. Each PASS entry carries a compact `proof`: the observed balances that make the check hold, with self-describing keys — both sides where two are compared, e.g. {\"ledger\":\"523500\",\"pool\":\"-523500\"} (drift), {\"left\":…,\"right\":…} (source_parity), {\"positive\":…,\"negative\":…} (ledger_invariant), or {\"balance\":…} (account_threshold). These reuse the FAIL balance keys, so under tolerance the residual (left−right) is visible without recomputation. Each FAIL entry carries the full `evidence` breakdown (balances, drift, tolerance, compiled CEL). Bounded by asset count (per-asset granularity; per-account is parked). An empty array means no outcomes were produced (no assets matched). For ERROR: an object describing the engine failure."""


EvidenceUnion = TypeAliasType("EvidenceUnion", Union[List[Evidence], Dict[str, Any]])
r"""For PASS/FAIL: the per-fingerprint roster. Each PASS entry carries a compact `proof`: the observed balances that make the check hold, with self-describing keys — both sides where two are compared, e.g. {\"ledger\":\"523500\",\"pool\":\"-523500\"} (drift), {\"left\":…,\"right\":…} (source_parity), {\"positive\":…,\"negative\":…} (ledger_invariant), or {\"balance\":…} (account_threshold). These reuse the FAIL balance keys, so under tolerance the residual (left−right) is visible without recomputation. Each FAIL entry carries the full `evidence` breakdown (balances, drift, tolerance, compiled CEL). Bounded by asset count (per-asset granularity; per-account is parked). An empty array means no outcomes were produced (no assets matched). For ERROR: an object describing the engine failure."""


class Result(str, Enum):
    PASS = "PASS"
    FAIL = "FAIL"
    ERROR = "ERROR"


class EvaluationTypedDict(TypedDict):
    created_at: datetime
    ended_at: datetime
    id: str
    result: Result
    rule_id: str
    started_at: datetime
    cost_units: NotRequired[int]
    error: NotRequired[str]
    evidence: NotRequired[EvidenceUnionTypedDict]
    r"""For PASS/FAIL: the per-fingerprint roster. Each PASS entry carries a compact `proof`: the observed balances that make the check hold, with self-describing keys — both sides where two are compared, e.g. {\"ledger\":\"523500\",\"pool\":\"-523500\"} (drift), {\"left\":…,\"right\":…} (source_parity), {\"positive\":…,\"negative\":…} (ledger_invariant), or {\"balance\":…} (account_threshold). These reuse the FAIL balance keys, so under tolerance the residual (left−right) is visible without recomputation. Each FAIL entry carries the full `evidence` breakdown (balances, drift, tolerance, compiled CEL). Bounded by asset count (per-asset granularity; per-account is parked). An empty array means no outcomes were produced (no assets matched). For ERROR: an object describing the engine failure."""
    pit_per_source: NotRequired[Dict[str, datetime]]
    r"""Effective historical PITs used by sources. For a Payments `latest` read, the value is the successful observation time because the upstream response has no snapshot timestamp; the evaluation evidence is frozen, but exact historical replay of that latest snapshot is not guaranteed."""


class Evaluation(BaseModel):
    created_at: Annotated[datetime, pydantic.Field(alias="createdAt")]

    ended_at: Annotated[datetime, pydantic.Field(alias="endedAt")]

    id: str

    result: Result

    rule_id: Annotated[str, pydantic.Field(alias="ruleID")]

    started_at: Annotated[datetime, pydantic.Field(alias="startedAt")]

    cost_units: Annotated[Optional[int], pydantic.Field(alias="costUnits")] = None

    error: Optional[str] = None

    evidence: Optional[EvidenceUnion] = None
    r"""For PASS/FAIL: the per-fingerprint roster. Each PASS entry carries a compact `proof`: the observed balances that make the check hold, with self-describing keys — both sides where two are compared, e.g. {\"ledger\":\"523500\",\"pool\":\"-523500\"} (drift), {\"left\":…,\"right\":…} (source_parity), {\"positive\":…,\"negative\":…} (ledger_invariant), or {\"balance\":…} (account_threshold). These reuse the FAIL balance keys, so under tolerance the residual (left−right) is visible without recomputation. Each FAIL entry carries the full `evidence` breakdown (balances, drift, tolerance, compiled CEL). Bounded by asset count (per-asset granularity; per-account is parked). An empty array means no outcomes were produced (no assets matched). For ERROR: an object describing the engine failure."""

    pit_per_source: Annotated[
        Optional[Dict[str, datetime]], pydantic.Field(alias="pitPerSource")
    ] = None
    r"""Effective historical PITs used by sources. For a Payments `latest` read, the value is the successful observation time because the upstream response has no snapshot timestamp; the evaluation evidence is frozen, but exact historical replay of that latest snapshot is not guaranteed."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["costUnits", "error", "evidence", "pitPerSource"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    Evaluation.model_rebuild()
except NameError:
    pass
