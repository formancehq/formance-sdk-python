"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from datetime import datetime
from formance_sdk_python.types import BaseModel, UNSET_SENTINEL
import pydantic
from pydantic import model_serializer
from typing import Dict, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class EvaluateRuleRequestTypedDict(TypedDict):
    at: NotRequired[datetime]
    r"""Default PIT for every source and canonical PIT for alert-period scoping. Defaults to now, but is required when `sourcePITs` is provided. Must be in the past. A supplied `at` is read point-in-time on the payments pool; the default (now) reads the pool's latest snapshot."""
    safety_margin: NotRequired[str]
    r"""Go duration string subtracted from the default `at` PIT. Defaults to 30s. It is not subtracted from already-effective `sourcePITs` replay values."""
    source_pi_ts: NotRequired[Dict[str, datetime]]
    r"""Optional per-source PIT overrides, keyed by the template's stable source key (\"<label>#<idx>\", as echoed back in Evaluation.pitPerSource — e.g. \"ledger:main#0\", \"pool:acct#0\"). A source with an override reads at exactly that instant instead of `at` (the safety margin is not applied again); this expresses the two-independent-timestamps contract (ledger at one instant, payments pool at another, to absorb inter-system settlement lag). `at` is required whenever this object is supplied so the evaluation has an unambiguous PIT for daily/monthly alert-period scoping. Every value must be in the past. A key that names no source of the rule's template is rejected with 400 (it is not silently ignored)."""


class EvaluateRuleRequest(BaseModel):
    at: Optional[datetime] = None
    r"""Default PIT for every source and canonical PIT for alert-period scoping. Defaults to now, but is required when `sourcePITs` is provided. Must be in the past. A supplied `at` is read point-in-time on the payments pool; the default (now) reads the pool's latest snapshot."""

    safety_margin: Annotated[Optional[str], pydantic.Field(alias="safetyMargin")] = None
    r"""Go duration string subtracted from the default `at` PIT. Defaults to 30s. It is not subtracted from already-effective `sourcePITs` replay values."""

    source_pi_ts: Annotated[
        Optional[Dict[str, datetime]], pydantic.Field(alias="sourcePITs")
    ] = None
    r"""Optional per-source PIT overrides, keyed by the template's stable source key (\"<label>#<idx>\", as echoed back in Evaluation.pitPerSource — e.g. \"ledger:main#0\", \"pool:acct#0\"). A source with an override reads at exactly that instant instead of `at` (the safety margin is not applied again); this expresses the two-independent-timestamps contract (ledger at one instant, payments pool at another, to absorb inter-system settlement lag). `at` is required whenever this object is supplied so the evaluation has an unambiguous PIT for daily/monthly alert-period scoping. Every value must be in the past. A key that names no source of the rule's template is rejected with 400 (it is not silently ignored)."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["at", "safetyMargin", "sourcePITs"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    EvaluateRuleRequest.model_rebuild()
except NameError:
    pass
