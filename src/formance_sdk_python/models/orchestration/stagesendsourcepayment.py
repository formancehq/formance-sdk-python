"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from formance_sdk_python.types import BaseModel, UNSET_SENTINEL
import pydantic
from pydantic import model_serializer
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class StageSendSourcePaymentTypedDict(TypedDict):
    id: str
    allow_overdraft: NotRequired[bool]
    r"""Enables unbounded overdraft on the throughAccount when set to true.
    Only applies when throughAccount is not \"world\" (which already has unbounded overdraft).

    """
    holding_account: NotRequired[str]
    r"""Intermediate account where payment funds are held.
    Defaults to \"payment:{paymentID}\" format.

    """
    ledger: NotRequired[str]
    r"""Ledger to use for payment ingestion.
    Defaults to the internal orchestration ledger.

    """
    through_account: NotRequired[str]
    r"""Source account for the payment ingestion transaction.
    Defaults to \"world\".

    """


class StageSendSourcePayment(BaseModel):
    id: str

    allow_overdraft: Annotated[
        Optional[bool], pydantic.Field(alias="allowOverdraft")
    ] = False
    r"""Enables unbounded overdraft on the throughAccount when set to true.
    Only applies when throughAccount is not \"world\" (which already has unbounded overdraft).

    """

    holding_account: Annotated[
        Optional[str], pydantic.Field(alias="holdingAccount")
    ] = None
    r"""Intermediate account where payment funds are held.
    Defaults to \"payment:{paymentID}\" format.

    """

    ledger: Optional[str] = None
    r"""Ledger to use for payment ingestion.
    Defaults to the internal orchestration ledger.

    """

    through_account: Annotated[
        Optional[str], pydantic.Field(alias="throughAccount")
    ] = "world"
    r"""Source account for the payment ingestion transaction.
    Defaults to \"world\".

    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["allowOverdraft", "holdingAccount", "ledger", "throughAccount"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    StageSendSourcePayment.model_rebuild()
except NameError:
    pass
